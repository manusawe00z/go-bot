# Environment Configuration
# Copy this file to .env and adjust settings

# Discord Bot Configuration
DISCORD_TOKEN=your_discord_token_here
COMMAND_PREFIX=!

# TTS Configuration
TTS_LANGUAGE=en
TTS_VOICE=

# Queue Configuration
QUEUE_SIZE=100
QUEUE_TIMEOUT_SECONDS=60

# Audio Quality (low, medium, high)
# - Use "medium" for development
# - Use "low" for production (better performance)
AUDIO_QUALITY=medium

# Logging Level (0=debug, 1=info, 2=warn, 3=error, 4=fatal)
# - Use level 0 (debug) for development
# - Use level 2 (warn) or 3 (error) for production
LOG_LEVEL=1

# Environment (development, production)
# - Controls certain optimizations
GO_ENV=development
